import styled from "styled-components";

const TitleMagyar = styled.h1`
  font-size: 3vw;
  color: var(--color-gray-light);

  @media (max-width: 800px){
    /* font-size: 40rem; */
  }
`;

export default TitleMagyar;
