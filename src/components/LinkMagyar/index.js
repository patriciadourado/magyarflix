import styled from "styled-components";

const LinkMagyar = styled.a`
  text-decoration: none;
  font-weight: bold;
  color: var(--color-gray-light);
  cursor: pointer;
`;

export default LinkMagyar;
